<Page x:Class="Pikto.Views.WizardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Width="{Binding ViewWidth}" Height="{Binding ViewHeight}"
	Title="WizardView" Background="HotPink">
    <Grid>
        <Label Content="Tytuł" Height="Auto" HorizontalAlignment="Left" Margin="12,12,0,0" Name="lTitle" VerticalAlignment="Top" FontSize="16" />
        <Button Content="&gt;&gt;&gt;" Height="23" HorizontalAlignment="Right" Margin="0,0,12,12" Name="bForward" VerticalAlignment="Bottom" Width="75" Command="{Binding ForwardCmd}" />
        <Button Content="&lt;&lt;&lt;" Height="23" HorizontalAlignment="Right" Margin="0,0,93,12" Name="bBack" VerticalAlignment="Bottom" Width="75" Command="{Binding BackCmd}" />
        <Button Content="Anuluj" HorizontalAlignment="Right" Margin="12,0,174,12" Name="bCancel" Width="75" Height="23" VerticalAlignment="Bottom" Command="{Binding CancelCmd}" />
        <Frame Margin="12,49,12,41" Name="stepContent" Padding="8" NavigationUIVisibility="Hidden" Focusable="False" />
    </Grid>
</Page>
