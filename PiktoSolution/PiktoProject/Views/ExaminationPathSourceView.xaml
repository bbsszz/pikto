<UserControl x:Class="Pikto.Views.ExaminationPathSourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:utils="clr-namespace:Pikto.Utils"
             mc:Ignorable="d"
             d:DesignHeight="90" d:DesignWidth="300">
    <Grid>
        <RadioButton Content="Konfiguruj nową ścieżkę" Height="16" HorizontalAlignment="Left" Margin="12,12,0,0" Name="rbNewPath" VerticalAlignment="Top" IsChecked="{Binding Path=Action, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static utils:ChooseEnum.New}}" />
        <RadioButton Content="Wybierz istniejącą ścieżkę" Height="16" HorizontalAlignment="Left" Margin="12,46,0,0" Name="rbLoadPath" VerticalAlignment="Top" IsChecked="{Binding Path=Action, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static utils:ChooseEnum.Existing}}" />
    </Grid>
</UserControl>
