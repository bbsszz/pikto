<Window x:Class="Pikto.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Pikto.Views"
        Title="Pikto" WindowStyle="None" ResizeMode="NoResize" FontFamily="Verdana" WindowState="Maximized" Top="0" AllowsTransparency="True" Height="300" Width="300" Background="SpringGreen">
    <Grid>
        <Frame Name="primaryContent" Content="{Binding Path=PrimaryViewType, Converter={StaticResource viewTypeConverter}}" />
        <Frame Name="secondaryContent" Content="{Binding Path=SecondaryViewType, Converter={StaticResource viewTypeConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="4" />
    </Grid>
</Window>